<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.example.eshop.mapper.OrderItemMapper">


    <!-- 新增訂單商品 -->
    <insert id="insertOrderItem"
            parameterType="com.example.eshop.entity.OrderItem"
            useGeneratedKeys="true"
            keyProperty="orderItemId">

        INSERT INTO orders_item
        (
            order_id,
            product_id,
            product_name,
            product_image,
            quantity,
            product_price
        )
        VALUES
        (
            #{orderId},
            #{productId},
            #{productName},
            #{productImage},
            #{quantity},
            #{productPrice}
        )

    </insert>



    <!-- 查詢某張訂單的商品 -->
    <select id="findByOrderId"
            parameterType="long"
            resultType="com.example.eshop.entity.OrderItem">

        SELECT
            order_item_id,
            order_id,
            product_id,
            product_name,
            product_image,
            quantity,
            product_price
        FROM order_item
        WHERE order_id = #{orderId}

    </select>


</mapper>